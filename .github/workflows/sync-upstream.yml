name: Sync Upstream Repository

on:
  schedule:
    - cron: '0 16 * * *'  # 每天 UTC 16:00 (北京时间 00:00)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Fetch upstream
        run: |
          git remote add upstream https://github.com/0xdabiaoge/singbox-lite.git 2>/dev/null || true
          git fetch upstream main --depth=1

      - name: Sync and check changes
        id: sync
        run: |
          git checkout upstream/main -- singbox.sh
          chmod +x singbox.sh
          
          if git diff --quiet HEAD singbox.sh; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "✅ singbox.sh 无变化"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "🔄 singbox.sh 已更新"
          fi

      - name: Commit and push
        if: steps.sync.outputs.has_changes == 'true'
        run: |
          git add singbox.sh
          git commit -m "🔄 同步上游 singbox.sh ($(date -u '+%Y-%m-%d'))"
          git push